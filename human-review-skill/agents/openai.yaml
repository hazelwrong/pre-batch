interface:
  display_name: "GDPval Human Review · Current"
  short_description: "Run current evidence-bound GDPval human review"
  default_prompt: "Use $human-review-gdpval to prepare and register the current staged GDPval human-review workflow without substituting model output for real reviewer decisions."
policy:
  allow_implicit_invocation: true
